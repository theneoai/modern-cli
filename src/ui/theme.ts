/**
 * CLI Theme - Re-export from unified theme system
 * @deprecated Import from '../theme/index.js' instead
 */

export {
  theme,
  chalkTheme,
  icons,
  formatHeader,
  formatSection,
  formatError,
  formatSuccess,
  formatWarning,
  formatInfo,
  formatTime,
  formatDate,
  truncate,
  wrapText,
  layout,
  palette,
} from '../theme/index.js';
