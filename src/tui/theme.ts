/**
 * TUI Theme - Re-export from unified theme system
 * @deprecated Import from '../theme/index.js' instead
 */

export {
  tuiTheme as theme,
  inkTheme,
  icons,
  formatTime,
  formatDate,
  truncate,
  wrapText,
  wrapTextLines,
  layout,
  palette,
} from '../theme/index.js';
